{
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "routes": [
      { "src": "/api/(.*)", "dest": "/api/contact.js" },
      { "handle": "filesystem" },
      { "src": "/(.*)", "dest": "/index.html" }
    ]
}